Project structure for the backend
```
├── backend               # New folder for the Flask backend
│   ├── app.py            # Flask application
│   ├── templates         # HTML files for Flask (if needed)
│   ├── static            # Static files served by Flask
│   │   ├── representative_cluster_TMAP.js
│   │   └── representative_cluster_TMAP.html
│   ├── Dockerfile        # Optional Docker setup
│   └── requirements.txt  # Backend dependencies (Flask, etc.)
```

